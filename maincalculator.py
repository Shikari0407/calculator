
import add_work

import sub_work

import mul_work

import div_work
a=300
b=10
total_sum =add_work.add(a,b)
total_diff=sub_work.sub(a,b)
total_product=mul_work.mul(a,b)
total_quotient=div_work.div(a,b)

print(f"Total sum is {total_sum}. Total diff is {total_diff}.Total pro is {total_product}")
